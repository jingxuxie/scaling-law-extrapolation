name: tests

on:
  push:
  pull_request:

permissions:
  contents: write

jobs:
  assemble-final-package:
    if: github.event_name == 'pull_request' && github.head_ref == 'agent/final-submission-package'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - name: Verify and apply final source patch
        shell: bash
        run: |
          set -euo pipefail
          cat .final-patch/part-* | base64 --decode | gzip -d > /tmp/final.patch
          expected=$(tr -d '[:space:]' < .final-patch/READY)
          actual=$(sha256sum /tmp/final.patch | awk '{print $1}')
          test "$actual" = "$expected"
          git apply --check /tmp/final.patch
          git apply --index /tmp/final.patch
          git rm -r .final-patch
          git rm .github/workflows/apply-final-patch.yml
          python - <<'PY'
          from pathlib import Path
          Path('.github/workflows/tests.yml').write_text('''name: tests

on:
  push:
  pull_request:

jobs:
  python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install
        run: python -m pip install -e ".[dev]"
      - name: Unit tests
        run: pytest -q
      - name: Reproduce exact-risk experiments
        run: python experiments/run_all.py
''', encoding='utf-8')
          PY
      - name: Publish clean assembled source tree
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "paper: assemble final submission sources"
          git push origin HEAD:${{ github.head_ref }}

  python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install
        run: python -m pip install -e ".[dev]"
      - name: Unit tests
        run: pytest -q
      - name: Reproduce exact-risk experiments
        run: python experiments/run_all.py
